[project]
name = "ha-abb-fimer-pvi-vsn-modbus"
version = "0.1.0"
description = "Home Assistant custom integration for ABB/FIMER PVI VSN Modbus"
readme = "README.md"
license = "MIT"
requires-python = ">=3.14.2"
authors = [{ name = "alexdelprete" }]

[project.urls]
"Homepage" = "https://github.com/alexdelprete/ha-abb-fimer-pvi-vsn-modbus"
"Bug Tracker" = "https://github.com/alexdelprete/ha-abb-fimer-pvi-vsn-modbus/issues"

[project.optional-dependencies]
dev = [
    "homeassistant>=2026.8.0",
    "homeassistant-stubs",
    "mdformat",
    "mdformat-gfm",
    "pre-commit",
    "pymarkdownlnt",
    "ruff>=0.8.0",
    "ty",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov",
    "pytest-homeassistant-custom-component>=0.13.0",
    "pytest-timeout",
]

[dependency-groups]
lint = [
    "homeassistant-stubs",
    "mdformat",
    "mdformat-gfm",
    "pre-commit",
    "pymarkdownlnt",
    "ruff>=0.8.0",
    "ty",
]
pytest = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov",
    "pytest-homeassistant-custom-component>=0.13.0",
    "pytest-timeout",
]
dev = [
    "homeassistant>=2026.8.0",
    { include-group = "lint" },
    { include-group = "pytest" },
]

# <!-- BEGIN SHARED:repo-sync -->
# <!-- END SHARED:repo-sync -->
